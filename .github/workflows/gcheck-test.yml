name: gcheck test

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Run basic checks
        run: |
          echo "Running gcheck test workflow..."
          echo "Repository is accessible"
          echo "Workflow infrastructure is working"
